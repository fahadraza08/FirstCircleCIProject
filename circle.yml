test:
 override:
 - ant -f builds/build.xml -lib build/lib/ deploy:
 timeout: 600

workflows:
  version: 2
  build-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master